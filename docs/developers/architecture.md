---
sidebar_position: 2
---

# Architecture

Famlin is a monorepo with these parts:

- **`backend/`** — Fastify + TypeScript + Prisma + Postgres API (ESM).
- **`backend/admin/`** — React + Vite admin UI, built into `backend/dist/admin/` and served by the backend at `/admin`.
- **`web/`** — desktop-focused React + Vite member-facing web app, built into `backend/dist/web/` and served by the backend at `/` (same single-container pattern as the admin UI). An npm-workspace member at the repo root together with `packages/api-client`.
- **`packages/api-client/`** — `@famlin/api-client`, the shared axios/data layer (auth, posts, comments, groups, notifications, media albums (provider-generic `media.ts` plus the legacy Immich-only `immich.ts`), invites, uploads, domain types, and the browser OIDC PKCE login flow used by `web/`'s login page) consumed by both `mobile/` (via a `file:` dependency) and `web/` (via the npm workspace). A storage-adapter seam (`setStorageAdapter`) lets each consumer plug in its own token persistence (SecureStore/AsyncStorage on mobile, `localStorage` on web).
- **`mobile/`** — Expo / React Native app for iOS and Android.
- **`docs/`** — Docusaurus documentation site (this website).

## Backend routes

| File | Prefix | Notes |
| --- | --- | --- |
| `auth.ts` | `/api/auth` | Generic OIDC (`/oidc`, `/oidc-config`) + local password login, `/me`, register/reset (admin; `POST /register` also accepts an optional `groupIds: string[]` — every id is validated to exist and the user plus its `GroupMember` rows are created atomically in one transaction), first-run admin bootstrap (`/setup-status`, `/setup`), public `/server-info` (`{ version, minAppVersion, appStoreUrl, playStoreUrl }` — `version` is the backend's own `package.json` version, shown in the mobile app's and web app's profile screens; `minAppVersion` is the oldest mobile app version this server still supports, bumped only when compatibility is genuinely dropped (a breaking change) so mobile can show a blocking update-required screen against it; `appStoreUrl`/`playStoreUrl` are this deployment's store links, from `Setting`, for that screen to link to — `null` when unset). `/oidc/exchange`, `/oidc/mobile-callback`, and `/oidc/mobile-handoff` support providers (e.g. Google) that require a client secret — see [OIDC / client-secret providers](#oidc--client-secret-providers) below |
| `groups.ts` | `/api/groups` | Read-only, member-facing: your groups, group detail, member list. List/detail responses also carry `allowedPostTypes: string[]` — the group's *resolved* effective list of creatable post types (never empty; see [Custom post types](#custom-post-types)) |
| `admin.ts` | `/api/admin` | All admin mutations: users, groups CRUD, membership, settings, cross-group content moderation (`/content/posts`, `/content/comments` — both take optional `groupId`, `authorId`, and `q` (case-insensitive content search) query filters), media album linking. Media/album mutations: `PATCH /media-albums/:id` body `{newAssetMode}` (OFF/MANUAL/AUTO); `GET /media/:provider/people` lists recognized people (returns array of `{id, name, thumbnailDataUri}` objects, 400 if provider lacks support); `GET /media/people-links` (array of links, each with optional `user` field); `POST /media/people-links` body `{provider, externalPersonId, label, userId?}` (upsert); `DELETE /media/people-links/:id`. `GET /post-types` lists the registered post types (`{ items: [{ id }] }`, see [Custom post types](#custom-post-types)); group create/update bodies accept an optional `allowedPostTypes: string[]` (each id validated against the registry, omitted/`[]` = all types including future ones), and admin group responses carry the **raw** stored array (unlike `groups.ts`'s resolved one) so the UI can tell "all" apart from an explicit list. `POST /content/posts/:id/retrigger-push` manually resends a post's push notification (push-only, see [Notifications](#notifications)); `GET /push-log?postId=` lists `PushDeliveryLog` rows (cursor-paginated), optionally filtered to one post |
| `posts.ts` | `/api/posts` | CRUD, always filtered by group membership. `GET /` is a filter over the caller's groups: `?groupIds=` (comma-separated subset) or the legacy `?groupId=` (one group), and no parameter means every group they belong to; requesting a group they're not in is a 403. The feed deduplicates cross-posted posts — a member of several of a post's target groups sees it once. Also `GET /search?groupId=&q=` (case-insensitive search over `content`/`milestoneTag`, still single-group) and `GET /on-this-day?groupId=` (posts from today's month/day in a prior year) — both registered before the dynamic `GET /:id` route. `POST /` also accepts `groupIds: string[]` (1-20 ids) instead of `groupId` to **cross-post**: with 2+ target groups the server creates one `Post` row per group sharing a `crossPostId`, after validating the caller is a member of every target (one non-member group → 403, nothing created). For the author these copies act as one post — `PATCH`/`DELETE` fan out to every copy — but comments, reactions, and favorites stay scoped to each group's own copy, and `crossPostId` is never exposed in API responses; only the author's own post responses carry `sharedWithGroups: [{ id, name }]` (every target group, including the copy's own), so other members can never tell a post was cross-posted. Photos sourced from a linked media album (Immich/local) are copied server-side into Famlin's own upload storage at post time — see [Photos and uploads](#photos-and-uploads) — while direct uploads are shared as-is; admin content moderation (`admin.ts`'s `/content/posts`) stays per-group, so deleting one group's copy doesn't touch the others. `type` is now an open string (`UPDATE`/`MILESTONE`/`POLL`, more later) looked up in the `PostTypeHandler` registry (see [Custom post types](#custom-post-types)); `POST` also accepts a handler-owned `typeData` (a poll's `{ options, closesAt? }`) — 400 `errors.unknownPostType`/`errors.invalidPostTypeData` for a bad type/typeData, 403 `errors.postTypeNotAllowed` if any target group's `allowedPostTypes` excludes it. Every list endpoint and `GET /:id` also return `typeData`, plus, for polls, an enriched `poll` object (`{ options: [{ id, text, voteCount, voters }], totalVotes, myVoteOptionId, closesAt, closed }`) — `POST`/`PATCH` responses carry `typeData` but not `poll` (same as `people`, they don't wait on the enrichment step). `POST /:postId/interactions` (`{ key, value }`) is the generic per-type action endpoint — poll voting today (`key: 'vote'`, `value: { optionId }`; the same option again unvotes, a different one switches; a closed poll 400s `errors.pollClosed`) — and returns the full shaped + enriched post; no domain event is emitted, so interactions never trigger notifications |
| `comments.ts`, `likes.ts`, `favorites.ts` | `/api` | Comments, likes, and favorites (`POST /posts/:postId/favorite` to toggle, `GET /favorites` for the logged-in user's bookmarked posts). `GET /posts/:postId/comments` accepts an optional `?assetUrl=` filter and `POST` accepts an optional `assetUrl` body field to pin a comment to one photo/video in the post instead of the post as a whole (replies inherit their parent's `assetUrl`). `POST` also accepts an optional `attachmentUrl` — a photo/video the commenter uploaded (`POST /api/uploads`) as part of the comment itself, distinct from `assetUrl` — and `content` is then optional, since `content`/`attachmentUrl` just need at least one present. `POST` also accepts an optional `mentionedUserIds` array (the client resolves "@name" against the group member list; the server re-validates membership and sends a `mention` notification instead of the generic `new_comment` one). Both `like` endpoints accept an optional `{ type }` body (a `ReactionType`, default `LIKE`) — see [Data model](#data-model) |
| `push-tokens.ts` | `/api/push-tokens` | `POST` registers this device's Expo push token; `DELETE` (called on logout) unregisters it |
| `api-tokens.ts` | `/api/api-tokens` | Self-service developer personal access tokens (`famlin_pat_...` bearer credentials for scripts/integrations — see [Using the API](./api)): `GET` lists the caller's tokens (no secrets), `POST` creates one (returns the plaintext exactly once; session-authenticated requests only — a PAT can't mint more PATs), `DELETE /:id` revokes. Verified in `plugins/auth.ts`'s `authenticate` decorator (SHA-256 hash lookup via `services/apiTokens.ts`) and accepted by `authenticateMediaRequest` for `/uploads/*`/Immich media. Managed from the web app's avatar menu (`ApiTokensModal.tsx`) — deliberately not in the mobile app |
| `notifications.ts` | `/api/notifications` | In-app notification history |
| `uploads.ts` | `/api/uploads` | Direct photo upload to a Docker volume, served at `/uploads/`; `GET /media-token` issues the short-lived token clients use to read it (see [Photos and uploads](#photos-and-uploads)) |
| `media.ts` | `/api/media` | Read-only, member-facing, **provider-generic** (see [Media integrations](#media-integrations)): `GET /groups/:groupId/albums` (a group's linked albums across every media source, each item carrying its `provider`), `GET /albums/:linkId/assets` (asset list with optional `?personId=` filter to show only assets of a mapped person; 404 if person isn't mapped, 400 if provider can't filter by person), `GET /groups/:groupId/photos` (merged photo timeline across linked albums + direct post uploads, cursor-paginated with optional `?personId=` filter; 403 non-member, 404 unmapped personId; an album item that a post embeds via its media-proxy URL carries that post's `postId` + the verbatim stored URL as `postAssetUrl`, so clients can offer like/comment/favorite on it), `GET /assets/:linkId/:assetId/:variantExt` (streams a rendition from whichever provider the link belongs to, gated the same dual-auth way as `/uploads/*` plus a check that the asset actually belongs to the linked album). New: `GET /api/media/people?groupId=` (lists people mapped to this group: `[{id, provider, label, userId?}]`, 403 if non-member; the list is deduplicated per provider+label, so same-label mappings appear once with a representative id). Linking/unlinking albums to a group is an admin mutation and lives in `admin.ts` instead |
| `immich.ts` | `/api/immich` | **Legacy compat, Immich-only**: same three endpoints as `media.ts` but scoped to `provider: 'immich'` links and generating `/api/immich/assets/...` URLs. Kept because that URL shape is stored in existing posts' `uploadedAssetUrls` and still generated by pre-built mobile clients — new clients should use `/api/media` instead. Both route files delegate to the same provider implementation |
| `invites.ts` | `/api/invites` | Public invite preview (`GET /:token`), self-service registration (`POST /:token/register`), and join-for-an-already-authenticated-user (`POST /:token/accept`). Invite creation/listing/revocation lives in `admin.ts` (`/api/admin/groups/:id/invites`, `/api/admin/invites/:id`) |
| `invite-landing.ts` | `/invite/:token` (no `/api` prefix) | Public server-rendered HTML page a shared invite link opens; hands off to the app via `famlin://invite/:token?server=...`. Matches the app's design (logo, teal palette), is translated using the server's `defaultLanguage` setting, and shows App Store/Google Play buttons if `appStoreUrl`/`playStoreUrl` are set |
| `landing.ts` | `/` (no `/api` prefix) | **Fallback only**: public server-rendered HTML landing page confirming the server is running, styled to match the invite landing page (shared HTML shell in `utils/html-page.ts`). Registered only when there is no `dist/web` build — normally the member-facing web app (`web/`) is served at `/` instead, with an SPA `index.html` fallback for client-side GET routes that aren't `/api/*`, `/uploads/*`, or `/admin*` (see the web-serving block in `app.ts`) |

Services live in `backend/src/services/` (`settings.ts`, `notifications.ts`, `invites.ts`, `users.ts`, `groups.ts`, `posts.ts`, `comments.ts`, `reactions.ts`, `onThisDay.ts`, `pagination.ts`, `apiTokens.ts`, plus three extension-point directories: the `media/` provider directory with `photoTimeline.ts` (in-memory merge of album + post photos with per-album caching) and `personFilter.ts` (shared person-filter logic); `notificationChannels/`; and `postTypes/` — see [Media integrations](#media-integrations), [Notifications](#notifications), and [Custom post types](#custom-post-types)). `src/events.ts` is the typed in-process domain event bus (`post.created`, `comment.created`, `reaction.added`): routes emit facts after their write commits, and subscribers in `src/subscribers/` (registered idempotently by `buildApp()`) decide what those facts mean — every handler runs fire-and-forget and error-trapped, so a broken subscriber can never fail or slow the emitting request. The notifications subscriber (`src/subscribers/notifications.ts`) owns all "who gets told what" policy; routes know nothing about notifications. `src/jobs/onThisDay.ts` runs daily and `src/jobs/newAssets.ts` runs hourly (minute 15, both via `node-cron`, scheduled in `server.ts`, not `app.ts`, so the test suite's `buildApp()` never triggers them): onThisDay sends each group a notification about posts made on that day in a previous year, while newAssets diffs linked albums against their provider and either notifies the group (MANUAL mode) or auto-creates a post (AUTO mode) when new assets appear.

**There is no seeded/default admin account.** `GET /api/auth/setup-status` returns `{ needsSetup: boolean }` — `true` iff the `User` table is empty — and the admin UI (`SetupPage.tsx`) shows a one-time account-creation screen instead of the login form while that holds. `POST /api/auth/setup` provisions that first account as an admin and only ever succeeds once: it runs inside a transaction holding a Postgres advisory lock (`pg_advisory_xact_lock`, see `SETUP_ADVISORY_LOCK_KEY` in `routes/auth.ts`) around a `user.count()` check, so two concurrent requests can't both pass the check and create two accounts — the loser gets `409`. `prisma/seed.ts` (`db:seed`) is a separate, dev/test-only fixture script (fake family members, sample posts, and a hardcoded `admin@example.com`/`test123456` account) — it's never invoked by the production Docker image or `prisma migrate deploy`, so don't rely on it for a real deployment's first-run flow.

List endpoints that can grow large (`GET /api/posts`, `GET /api/favorites`, and the admin users/content-moderation endpoints) are cursor-paginated: they accept `?cursor=` and `?take=` (default 30, max 100) and respond with `{ items, nextCursor }` instead of a bare array. `services/pagination.ts` has the shared `paginationArgs`/`paginate` helpers.

## Data model

The Prisma schema defines `User`, `Setting`, `Group`, `GroupMember`, `Post`, `PostInteraction`, `Comment`, `Like`, `Favorite`, `PushToken`, `Notification`, `PushDeliveryLog`, `Invite`, `MediaAlbumLink`, `MediaPersonLink`, and `ApiToken`.

Key choices:

- `GroupMember` is a many-to-many join table (`@@unique([groupId, userId])`).
- Every `Post` belongs to exactly one `Group`. `Post.crossPostId` (nullable, indexed) links the copies created by a cross-post (`POST /api/posts` with `groupIds`) — one `Post` row per target group, sharing the same `crossPostId`. It's an internal linking key only: never returned in API responses (the author-only `sharedWithGroups` field on `Post` serves that purpose instead — see the `posts.ts` route row above).
- `Post.type` is an open string (`@default("UPDATE")`, was a closed `PostType` enum) — an unregistered value means an unrecognized `PostTypeHandler`, not a data-integrity problem, since routes 400 on creation before that could ever happen. `Post.typeData` (nullable `Json`) is handler-owned, type-specific config — `null` for `UPDATE`/`MILESTONE`, a poll's `{ options: [{ id, text }], closesAt }` for `POLL` — validated/transformed only at creation (immutable afterward). `PostInteraction` is a generic per-user, per-post state table (`@@unique([postId, userId, key])`, indexed on `[postId, key]`) — a poll vote today (`key: 'vote'`, `value: { optionId }`), future interaction types (RSVPs, ...) later — rather than one bespoke column/table per feature. `Group.allowedPostTypes` (`String[]`, `@default([])`) restricts which post types members may create in that group; empty means "every registered type," the same convention as `Setting.allowedEmails` — see [Custom post types](#custom-post-types).
- `Favorite` is a user's personal bookmark on a post (`@@unique([postId, userId])`), the same shape as `Like` but with its own overview (`GET /api/favorites`) instead of a public count.
- `Like` is a reaction, not a plain boolean: `type` (`ReactionType`: `LIKE`/`LOVE`/`HAHA`/`WOW`/`SAD`/`CARE`, default `LIKE`) holds which emoji a user picked. The `@@unique([postId, userId])`/`@@unique([commentId, userId])` constraints still cap it at one reaction per user per post/comment — switching reactions updates that row instead of adding a second one, and tapping the same reaction again deletes it. Post responses also carry `recentReactors` — the three most recent reactors' `{ id, name, avatarUrl }`, newest first (see `shapePost` in `services/posts.ts`) — so clients can show *who* reacted (the mobile app's overlapping reaction avatars), not just a count. Post responses also carry `people` — an array of admin-mapped media people recognized in that post's photos, computed read-time in `services/media/personTags.ts` by querying each linked album's asset-to-people index (Immich metadata search + per-asset detail fallback, 10-minute cache) and merging tagged assets with the same person-label into one chip, with in-memory caching and fail-soft semantics (provider outage or no matched people → empty array, never a failed request).
- `Notification.type` is a plain string, not a DB enum — the closed set of values (`new_post`, `new_comment`, `new_like_post`, `new_like_comment`, `mention`, `on_this_day`) lives in `NotifyType` in `services/notifications.ts`, along with which of `User`'s six email/push preference columns each type checks. `mention` and `on_this_day` reuse the comment/post columns rather than adding two more.
- `User.isAdmin` guards admin-only actions. The admin UI refuses to demote or remove the last remaining admin (`isLastAdmin` in `routes/admin.ts`).
- `User`, `Post`, and `Comment` are all hard-deleted — there's no `deletedAt` column on any of them. `DELETE /api/admin/users/:id` calls `prisma.user.delete`, which cascades to remove that user's posts, comments, likes, favorites, and notifications immediately (`onDelete: Cascade` on the relevant relations). `DELETE /api/posts/:id`/`DELETE /api/comments/:id` are permanent the same way. None of these can be undone — there's no restore.
- `User.tokenVersion` is embedded in every JWT and checked on every authenticated request; incrementing it (on password change/reset) immediately invalidates every token issued before that point, without needing a token blocklist.
- `ApiToken` is a developer personal access token (see [Using the API](./api)): only a SHA-256 `tokenHash` of the `famlin_pat_...` secret is stored (`tokenPreview` keeps the first few characters for display), with optional `expiresAt` and a throttled `lastUsedAt`. It's deliberately **independent of `User.tokenVersion`** — a password reset signs out app sessions but keeps integrations running; revocation is deleting the row (deleting the user cascades). Because the row itself is the credential, `verifyApiToken` checks existence + expiry on every request, so revocation is immediate.
- `Setting` stores runtime configuration such as the OIDC issuer/client ID, allowed emails, SMTP credentials, the server's `defaultLanguage`, and optional `appStoreUrl`/`playStoreUrl`. `appStoreUrl` is blank by default (no single iOS listing — each deployment distributes its own build). `playStoreUrl` defaults to the official pre-built Android app (`https://play.google.com/store/apps/details?id=be.xeweb.famlin`, see `DEFAULT_PLAY_STORE_URL` in `services/settings.ts`) whenever the setting has never been saved — an admin can still blank it out explicitly to hide the download button.
- `Comment.assetUrl` (nullable) pins a comment to one entry in the post's `uploadedAssetUrls` instead of the post as a whole — `null` means a post-level comment. Used by the mobile image viewer's per-photo comment thread.
- `Comment.attachmentUrl` (nullable) is a photo/video the commenter uploaded as part of the comment itself (an `/uploads/` path, like `Post.uploadedAssetUrls`) — distinct from `assetUrl` above, which instead points at an *existing* asset already on the post. A comment can carry an `attachmentUrl` with no `content` (photo/video-only), but not neither.
- `Invite` is a single-use, optionally-expiring, optionally-email-restricted token an admin generates per group (admin UI: Groups page). A valid invite is its own authorization: it lets `POST /api/auth/oidc` and `POST /api/auth/login` provision/join a user even when their email isn't on the `allowedEmails` whitelist (see `plugins/auth.ts`'s `verifyOidcToken(idToken, { allowUnlisted })`), and it lets `POST /api/invites/:token/register` create a local account without going through the normally admin-only `POST /api/auth/register`.
- `MediaAlbumLink` links one external media album to one Famlin group: `provider` says which media source it lives on (`'immich'`, `'local'`, ...) and `externalAlbumId` is that provider's album identifier (an Immich album uuid, a local folder name), with `@@unique([groupId, provider, externalAlbumId])`. `newAssetMode` controls how new assets on this album are surfaced (`OFF` = never, `MANUAL` = a notification reusing the new-post preference columns, `AUTO` = an auto-created post in the group feed); `newAssetsCheckedAt` is the last time the new-assets job ran for this link, used to diff against the provider — see [Media integrations](#media-integrations). (It was born as `ImmichAlbumLink`; the `20260709100000_generalize_media_album_link` migration renamed it and backfilled `provider: 'immich'`.)
- `MediaPersonLink` maps a provider's face/person identity to a display label and optionally a Famlin user: `provider`/`externalPersonId` unique, `label` shown to members, `userId` nullable. Only admin-mapped people are visible to members and enable per-person asset filtering (`?personId=` on album-asset listing).

## Configuration

Runtime config lives in the database, not in `.env`. Only `DATABASE_URL`, `PORT`, `NODE_ENV`, `JWT_SECRET`, and `TRUST_PROXY` are startup environment variables — see [Server setup](/docs/server-setup) for `TRUST_PROXY`. `getAllSettings()` (in `services/settings.ts`) caches the `Setting` table in memory for 10s, invalidated immediately on `updateSettings()`, since it's read on nearly every request.

## OIDC / client-secret providers

The default OIDC flow (`POST /oidc`) is a public-client PKCE exchange the client (web app, admin browser, or mobile app) does directly against the provider, with `redirect_uri` on that client's own origin (`https://.../` for the web app, `https://.../admin/` for the admin UI, or the app's `famlin://` scheme) — no secret ever touches Famlin's backend. Authentik/Keycloak/Auth0-style self-hosted providers support this.

Google does not: its OAuth clients require a `client_secret` for the code→token exchange, and its native/"iOS"/"Android" client types force redirect_uri onto a Google-generated `com.googleusercontent.apps.<id>` scheme rather than the app's own — a hard blocker for Famlin's mobile app specifically, since it ships one shared pre-built binary (`be.xeweb.famlin` on Google Play) used by many independent self-hosted deployments, each with a different Google OAuth client and therefore a different mandated scheme; the scheme can't be baked into the shared binary per-deployment. The fix is an optional `oidcClientSecret` setting (`services/settings.ts`) that switches both surfaces to a server-mediated exchange, letting Google be configured as a single ordinary **Web application** OAuth client instead:

- **Web app & admin UI**: unchanged redirects (`https://.../` and `https://.../admin/`, real HTTPS URLs Google already accepts) — but instead of exchanging the code itself, the SPA POSTs `{ code, redirectUri, codeVerifier }` to `POST /oidc/exchange`, which holds the secret and does the exchange server-side (each SPA's `LoginPage.tsx`, gated on `oidcConfig.usesClientSecret` from `GET /oidc-config`; the web app calls it via `@famlin/api-client`'s `exchangeOidcCode()`).
- **Mobile app**: can't use an HTTPS `redirect_uri` directly — the OS won't hand control back to the app for a plain HTTPS redirect (no universal links/App Links setup). Instead the authorize request's `redirect_uri` is `mobileCallbackUrl` (`GET /oidc-config`'s `${origin}/api/auth/oidc/mobile-callback`, under the deployment's own domain, which Google is happy to accept), and `GET /oidc/mobile-callback` completes the login server-side, then 302s to `famlin://oidc-callback?handoff=<code>` — a scheme the app already owns and `WebBrowser.openAuthSessionAsync` is watching for (`utils/oidcLogin.ts`). The Famlin session token isn't put directly in that redirect's query string (would leak into the server's own access logs); instead it's stashed in a short-lived, single-use, in-memory handoff (`services/oidcHandoff.ts`, 2-minute TTL) that the app immediately redeems via `POST /oidc/mobile-handoff`.
- An in-progress invite claim (`InviteScreen.tsx`'s SSO button) has no session to travel through for the mobile flow, so it's round-tripped through the OAuth `state` parameter as plain JSON (`{"inviteToken":"..."}`) instead — trust here matches the existing direct-PKCE flow, which already accepts a client-supplied `inviteToken` unsigned, since the invite's own validity check is what's actually authoritative server-side.
- `completeOidcLogin()` (`services/oidcLogin.ts`) is the shared user-provisioning/token-issuing logic behind all three entry points (`POST /oidc`, `POST /oidc/exchange`, `GET /oidc/mobile-callback`), so the invite/allowedEmails/account-creation rules only exist once.

## Authorization rules

- Group membership is the core authorization rule. Every posts query must filter on groups the logged-in user belongs to — `services/groups.ts`'s `isGroupMember(groupId, userId)` is the shared check most routes use.
- Group mutations live only in `admin.ts` and require `isAdmin`.
- `requireAdmin` returns a boolean and sends the 403 itself. Callers must `return` when it returns `true`.
- The global error handler (`app.ts`) maps a thrown `ZodError` to a `400` and hides any other unhandled error behind a generic translated message — route handlers shouldn't leak `err.message` to the client for anything that isn't already a deliberately user-facing, translated string.

## Internationalization

Both the mobile app and the admin UI use `i18next` + `react-i18next`. Locales live in:

- `mobile/src/i18n/locales/{en,nl}.json`
- `backend/admin/src/i18n/locales/{en,nl}.json`

`en.json` is the source, `nl.json` the translation. `fallbackLng` is `en`.

## Photos and uploads

Photos are uploaded directly to the Famlin backend (`POST /api/uploads`) and stored in a Docker volume (`famlin-uploads`). `uploadedAssetUrls`/`assetUrl` fields are validated against the exact `/uploads/<uuid>.<ext>` shape the upload route writes, so a post/comment can't reference an arbitrary external URL.

Cross-posting (`POST /api/posts` with `groupIds`, see the `posts.ts` route row above) needs an extra step for provider-sourced photos: a media-proxy asset URL (`/api/media/assets/<linkId>/...`) embeds the linked album's `linkId`, and that album's link only belongs to one of the post's target groups — the others have no access to it. So at post-creation time, any such asset is downloaded server-side and re-saved into `famlin-uploads` as an ordinary `/uploads/<uuid>.<ext>` file (one copy shared by every target group's `Post` row), rather than being referenced by proxy URL. The source album's link must itself belong to one of the target groups, same as the single-group cross-group check described in [Media integrations](#media-integrations). Direct `/uploads/...` uploads need no copying — they're already provider-agnostic files usable by any group's `Post` row.

`/uploads/*` is not public: an `onRequest` hook in `app.ts` requires either a normal session token (`Authorization` header) or a **media token** before `@fastify/static` serves the file. A media token is a separate, narrower-scope JWT (`scope: 'media'`, 7-day expiry, `createMediaToken`/`verifyMediaToken` in `plugins/auth.ts`) issued via `GET /api/uploads/media-token` — clients fetch one on login and pass it as a `?token=` query param (mobile: `getUploadUrl()` in `api/uploads.ts`), since `<Image>`/video sources can't attach custom headers. It's deliberately not the main session token, so it isn't the thing exposed in every image URL / proxy log. Both token types are checked against the DB in that hook (`isSessionCurrent`), so — like every other authenticated request — a deleted account or a token from before a password change/reset loses `/uploads` access immediately rather than at natural token expiry; the media token embeds the user's `tokenVersion` at issue time so it's revoked the same way. `authenticateMediaRequest()` in `plugins/auth.ts` factors this check out so `routes/immich.ts`'s asset proxy (below) can reuse it verbatim instead of duplicating the header/query-token logic.

## Media integrations

External photo sources are **media providers**: implementations of the `MediaProvider` interface (`services/media/types.ts`) registered in the static registry `services/media/registry.ts`. Adding a source (Nextcloud, PhotoPrism, ...) = implementing that interface in a new file and adding it to the registry — `MediaAlbumLink.provider` stores the registry ids, so they must never be renamed once deployments have linked albums. Two providers ship today:

- **`immich`** (`services/media/immich.ts`) — wraps the Immich REST API (`x-api-key` auth) using one server-level connection (server URL + API key, admin UI's Server Settings, stored in `Setting` like the OIDC config). Rather than every family member creating their own Immich API key, an admin links specific Immich albums to specific Famlin groups — regular members never see an Immich login, token, or API key.
- **`local`** (`services/media/local.ts`) — serves photos straight from a directory on disk (the `localMediaPath` setting; typically a NAS share bind-mounted into the container). Every immediate subdirectory of the root is a linkable "album", the image files directly inside it are its assets (asset id = base64url of the filename, so arbitrary filenames survive the strict URL charset), and `thumbnail`/`preview` renditions are generated with `sharp` into an on-container `media-cache/` directory (regenerated when the source file changes; losing the cache just means regenerating on demand). Deliberately images-only for now — video thumbnails would need ffmpeg. Album ids are validated against path traversal both at link time (`isValidAlbumId`) and again when resolving directories.

Providers throw a typed `MediaProviderError` (`not_configured`/`unreachable`/`unauthorized`) the same way `plugins/auth.ts`'s `OidcError` does, so routes can map it to a translated message instead of leaking fetch/fs internals; `mediaErrorKey()`/`mediaErrorStatus()` in the registry are the single mapping every route imports rather than re-deriving. Linking is an admin mutation (`GET`/`POST /api/admin/groups/:id/media-albums`, `DELETE /api/admin/media-albums/:id`, catalog via `GET /api/admin/media/:provider/albums`, connection tests via `POST /api/admin/immich/test` / `POST /api/admin/media/local/test`).

A provider-sourced photo is represented as just another string in `Post.uploadedAssetUrls` — a proxy URL shape (`/api/media/assets/<linkId>/<assetId>/<variant>.<ext>`, or the legacy `/api/immich/assets/...` equivalent) rather than the usual `/uploads/<uuid>.<ext>` one, validated by `parseMediaAssetPath()` in `types.ts` — the single place both shapes are defined, reused by the zod validator, the routes' `:variantExt` checks, and `routes/posts.ts`'s cross-group check. Because it's "just another asset URL," every feature that already operates on `uploadedAssetUrls` — feed rendering, the image viewer, per-photo comment pinning (`Comment.assetUrl`) — needs no provider-specific code. The extension reflects the real bytes each variant streams: `thumbnail`/`preview` are always `.jpg`, while `original` matches the asset's real type (`.mp4` for an Immich video, the actual image extension for a local file) — that's also why the mobile picker attaches `originalUrl` (not `previewUrl`) for a video, so `isVideoUrl()` and the `<Video>` player it feeds get an actually-playable file. `streamAsset()` implementations must honor the client's `Range` header and relay/produce `206`/`Content-Range`/`Accept-Ranges` responses — native video players (iOS AVPlayer, Android ExoPlayer) stream mp4s via byte-range requests and refuse to play when the server ignores them, while web `<video>` tolerates a full-body `200`, so dropping `Range` breaks video only on mobile.

Authorization for the asset proxy routes resolves `linkId → groupId`, checks the requester is a member of that group, and then calls the provider's `isAssetInAlbum()` to confirm the requested `:assetId` is actually one of that album's assets (Immich caches this album listing in memory for 60 seconds — single-instance, matching `getAllSettings()`'s settings cache — so an image grid doesn't re-query Immich per tile; the local provider stats the file inside the album directory). That re-check is part of the `MediaProvider` contract, not an optimization: a provider's server-level credential (Immich's API key) or source-wide root (the local folder) can read anything on the source, so without it a member who learns an asset id from elsewhere (e.g. another group's post) could read assets never linked to Famlin at all, not just ones linked to a different group. `routes/posts.ts`'s `POST /` additionally checks, for any media proxy URL in `uploadedAssetUrls`, that the URL's embedded `linkId` belongs to the post's own `groupId` — otherwise a member of one group could attach another group's linked album photos to a post in a group they share only incidentally.

## Custom post types

A post's behavior beyond the universal update/comment/reaction shell is pluggable: `Post.type` is an open string discriminator, and each type is a `PostTypeHandler` (`services/postTypes/types.ts`) registered in the static registry `services/postTypes/registry.ts` — modeled directly on the `MediaProvider` pattern above. Adding a type (an RSVP, ...) = implementing that interface in one new file and adding it to the registry map; `Post.type` stores these ids, so — like `MediaAlbumLink.provider` — they must never be renamed once a deployment has posts of that type. Three ship today, one file each:

- **`update`** (`services/postTypes/update.ts`) and **`milestone`** (`services/postTypes/milestone.ts`) — trivial handlers (`{ id: 'UPDATE' }`/`{ id: 'MILESTONE' }`) with no `typeDataSchema`, so sending `typeData` for either is rejected. `milestoneTag` stays its own legacy top-level `Post` column, untouched by this framework.
- **`poll`** (`services/postTypes/poll.ts`) — `content` holds the poll's question (so search, notification previews, and old clients keep working; an old client just renders a poll as a plain text post). `typeDataSchema` validates the client-sent create payload (2-10 options, 1-100 chars each, optional `closesAt`); `transformCreate` assigns each option a stable `crypto.randomUUID()` id before persisting (options are immutable afterward, since votes reference an option by id); `interact()` handles `POST /api/posts/:postId/interactions` for `key: 'vote'` — same option again unvotes, a different option switches, a closed poll (`closesAt` in the past) or unknown option 400s. Votes are `PostInteraction` rows (one per `(post, user, 'vote')`) and are **public** to group members (non-anonymous) — results are always visible, before or after voting.

A `PostTypeHandler` has four optional hooks beyond its `id` and `typeDataSchema`: `validateCreate` (semantic checks beyond zod, e.g. a poll's non-empty question), `transformCreate` (client payload → persisted `typeData`), `interact` (handle a `POST /:postId/interactions` call for this type), and `enrichPosts` (batch-attach computed fields — poll vote tallies — to a page of already-shaped posts of that type in one query, no N+1; called from `shapePostsWithPeople` in `services/posts.ts` alongside the people-tagging enrichment, so every list endpoint and `GET /:id` go through it automatically). Both `validateCreate`/`interact` throw a typed `PostTypeError` (`code` is an i18n error-key suffix, e.g. `'pollClosed'` → `t('errors.pollClosed')`) the same way `MediaProviderError` lets a route translate a failure without leaking internals.

`Group.allowedPostTypes` restricts which of the registered types members may create in that group (empty = all, including types registered in a later server version); `resolveAllowedPostTypes(group)` in the registry resolves that convention, and `routes/posts.ts`'s `POST /` checks every target group's resolved list before creating any row (a cross-post needs **all** targets to allow the type). Enforcement is creation-time only — a post of a type later disallowed for its group stays visible.

## Notifications

Domain events (`src/events.ts`) feed the notifications subscriber (`src/subscribers/notifications.ts`), which decides who gets told what — thread participants for comments, validated mentions, the target author for reactions — and calls `services/notifications.ts`. `notify()` always writes the in-app `Notification` history row, then fans out to **notification channels**: implementations of the `NotificationChannel` interface in `services/notificationChannels/` (per-channel `isEnabled()` server switch, per-recipient `wants()` preference check, `send()` delivery), registered in that directory's `index.ts`. Adding a delivery mechanism (Telegram, ntfy, ...) = one new file there. Channels run in parallel with per-channel error trapping, so one broken mechanism never blocks the others. Two ship today:

- **`push`** via Expo's push service (requires a development/production build).
- **`email`** via Nodemailer + SMTP, configurable per user with `emailOnNewPost`/`emailOnNewComment`/`emailOnNewLike`.

Both respect group membership: only members of the relevant group are notified, because the subscriber derives recipients from `GroupMember` before any channel runs.

**Push delivery logging.** Every push send attempt (one Expo API call, organic or admin-triggered) writes a `PushDeliveryLog` row — `sendPush()` in `services/notificationChannels/push.ts` is the single write path, called both by `pushChannel.send()` (the organic `notify()` flow) and directly by `resendPostPush()` in `services/notifications.ts` (see below). Each row records `postId`, `notifyType`, `recipientCount` (who was eligible), `tokenCount` (registered Expo tokens actually attempted), `successCount`/`failureCount` (per-ticket Expo results), and `triggeredByAdminId` (null for organic sends). Viewable in the admin UI's "Push notification log" page, backed by `GET /api/admin/push-log`.

**Manual resend.** An admin can resend a specific post's push notification (e.g. a member's device was offline when it first fired) from the content-moderation UI — `POST /api/admin/content/posts/:id/retrigger-push` calls `resendPostPush()`, which re-derives the post's group members (excluding the author), re-resolves the `new_post` message, and calls `sendPush()` directly. It's push-only: it doesn't touch email and doesn't duplicate the in-app `Notification` row from the original event. 400s `errors.pushNotificationsDisabled` if push is off server-wide.
