{"schema":{"type":"object","description":"The enriched, read-time view of a `TRIP` post's `typeData` — computed\nfresh on every read, never stored as-is. A trip's check-ins are its\n`Comment` rows with `metadata.kind: \"trip_checkin\"` (see `Comment`\nbelow); this object aggregates them rather than requiring a second\nfetch.\n","properties":{"title":{"type":"string"},"destination":{"type":"string","nullable":true},"startDate":{"type":"string","example":"2026-08-01","description":"Plain `YYYY-MM-DD`, no time component."},"endDate":{"type":"string","nullable":true,"example":"2026-08-14"},"coverPhotoUrl":{"type":"string","nullable":true},"travelers":{"type":"array","items":{"type":"object","properties":{"id":{"type":"string"},"name":{"type":"string"},"avatarUrl":{"type":"string","nullable":true}},"title":"UserSummary"},"description":"Co-travelers who may also check in, resolved from\n`typeData.travelerUserIds` — never includes the post's own\nauthor, who is an implicit traveler shown separately via\n`author`.\n"},"closed":{"type":"boolean","description":"`true` once the trip was explicitly closed (`close` interaction)\nor its `endDate`'s day has fully elapsed (auto-close) — either\nway, `checkin`/`close`/`setTravelers` are then rejected.\n"},"closedAt":{"type":"string","format":"date-time","nullable":true,"description":"When the trip was explicitly closed; `null` if it's still open or only auto-closed by `endDate`."},"dayNumber":{"type":"integer","nullable":true,"description":"Which day of the trip today is (1-based), or `null` once `closed` or while `startDate` is still in the future."},"durationDays":{"type":"integer","nullable":true,"description":"Total trip length in days, or `null` while still open with no `endDate`."},"stopCount":{"type":"integer","description":"Total number of check-ins."},"photoCount":{"type":"integer","description":"Total number of photos across every check-in."},"latestCheckin":{"type":"object","nullable":true,"properties":{"commentId":{"type":"string"},"place":{"type":"string"},"createdAt":{"type":"string","format":"date-time"}}},"collagePhotoUrls":{"type":"array","items":{"type":"string"},"maxItems":3,"description":"Up to the 3 most recent check-in photos, newest first."}},"title":"PostTrip"},"schemaType":"response"}